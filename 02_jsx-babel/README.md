# Vorraussetzungen

* NodeJs + npm
* Babel CLI
* Babel preset für React

cmd/shell
```
npm install babel-cli babel-preset-react --save-dev
```

.babelrc
```
{
  "presets": ["react"]
}
```